
export const getRouterData = (app) => {
	console.log('getRouterData app=>', app)
	const routerConfig = {
		'/': {
			component: null,
		}
	}

	const routerData = {}
	// The route matches the menu
	return routerData
}